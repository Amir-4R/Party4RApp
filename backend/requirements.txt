# =============================================================================
# Party4RApp — FastAPI Backend Dependencies
# Minimal set — every package below is actually imported by server.py.
# =============================================================================

fastapi==0.110.1            # HTTP framework + WebSocket support
uvicorn[standard]==0.25.0   # ASGI server (includes uvloop, httptools, websockets)
motor==3.3.1                # Async MongoDB driver (depends on pymongo)
pydantic>=2.6.4             # Request/response models (also pulled by FastAPI)
python-dotenv>=1.0.1        # Loads .env files for local dev
bcrypt==4.1.3               # Password hashing
pyjwt>=2.10.1               # JWT token signing/verification
httpx>=0.27.0               # Async HTTP client for YouTube Data API v3 calls
python-multipart>=0.0.9     # FastAPI form parsing (used by OAuth2PasswordBearer)
